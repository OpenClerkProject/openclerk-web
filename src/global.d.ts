// Injected by scripts/build.js via esbuild's `define`, from node_modules/openclerk-core's own
// package.json at build time -- reflects whatever openclerk-core build actually got bundled, not
// just this project's own package.json version range.
declare const __OPENCLERK_CORE_VERSION__: string;
